const solution = (str1, str2) => {
  const lt = 0;
  const rt = str2.length;
  const map = new Map();
  // map에 값 1 -1 저장
  for (let i = lt; i < rt; i++) {
    map.set(str1[i], 1);
  }
  for (let i = lt; i < str1.length; i++) {

  }
};

console.log(solution());
